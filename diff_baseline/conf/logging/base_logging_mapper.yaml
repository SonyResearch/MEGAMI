
log: True #to log or not to log

log_interval: 100 #how often to log simple stats
heavy_log_interval: 5000 #how often to log heavy info (audio, spectrograms, etc)

save_model: True #whether to save the model or not
save_interval: 5000 #how often to save the model

remove_old_checkpoints: False #whether to remove the old checkpoints or not, useful to save space (I am always at the limit of my quota)

#for logging the loss by sigma plot
num_sigma_bins: 10

print_model_summary: False #whether to print the model summary or not

log_audio: True #whether to log audio or not
num_audio_samples_to_log: 8 #how many audio samples to log

num_train_samples_to_log: 8

log_spectrograms: False
#stft for plotting spectrograms
stft:
  win_size: 1024
  hop_size: 256

wandb:
  entity: "eloimoliner"
  project: "project_mapper"
